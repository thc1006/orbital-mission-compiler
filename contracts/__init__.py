# contracts/ — Interface contracts for ORCHIDE-compatible ground system components.
# These are type definitions only. No runtime implementation.
#
# Note: this package lives at the repo root, not under src/.
# It is intentionally NOT part of the distributable `orbital-mission-compiler`
# package (pyproject.toml only discovers src/). Contracts are reference
# specifications, not production API. Use PYTHONPATH=src:. for local testing.
